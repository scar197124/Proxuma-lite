Proxuma Rebuild v5 Floor Fixed

This build restores the fuller v3 layout and fixes the last regression.
Changes:
- keeps full Home / About / Scanner layout
- keeps centered header system
- keeps full glass cards and scanner result layout
- patches only the floor/background gradient and color consistency
- does not strip the stylesheet again
